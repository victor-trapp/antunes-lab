apiVersion: v1
kind: Service
metadata:
  name: edge-status-api
  namespace: trapp-lab
  labels:
    app: edge-status-api
spec:
  type: NodePort
  selector:
    app: edge-status-api
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30080