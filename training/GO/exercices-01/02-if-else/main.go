package main

import "fmt"

// Exercise 02 — If / Else
//
// Go's if/else works like most languages with two differences:
// - The condition does NOT need parentheses
// - The braces `{}` are REQUIRED (no single-line shortcuts)
// You can also declare a short variable inline: `if x := f(); x > 0 { ... }`
// That variable is scoped to the if/else block only.
//
// Why this matters: conditional logic is the backbone of any program.
// The inline variable trick keeps scope tight and reduces clutter.
//
// TODO:
// 1. Declare a variable `score` and assign it any integer value
// 2. Write an if/else that prints "Pass" if score >= 50, otherwise prints "Fail"
// 3. BONUS: add an `else if` branch that prints "Excellent" if score >= 90

func main() {
	// Your code here
	_ = fmt.Println // remove this line once you use fmt
}
